default_stages: [commit]
exclude: "^(data/|derived/|.venv/|.code/)"
repos:
  - repo: local
    hooks:
      - id: ruff
        name: ruff
        entry: uvx ruff check
        language: system
        pass_filenames: true
        types: [python]
      - id: ruff-format
        name: ruff-format
        entry: uvx ruff format
        language: system
        pass_filenames: true
        types: [python]
      - id: mypy
        name: mypy
        entry: uvx mypy src
        language: system
        pass_filenames: false
